# Source host -> redirect target, one per line.
# Baked into the image as the default table; overridden when a
# deployment mounts a mappings.env at the override path.
redirect-bcit.ltc.bcit.ca=https://bcit.ca
