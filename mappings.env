# Source host -> redirect target, one per line.
# Default table baked into the image; ignored when REDIRECT_MAPPINGS is
# set (e.g. by the deployment ConfigMap, which Flux renders from values).
redirect-bcit.ltc.bcit.ca=https://bcit.ca
